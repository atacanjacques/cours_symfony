{% extends 'base.html.twig' %}

{% block body %}
Index Post

<table>
{% for post in posts %}
<tr>
  <td>{{ post.id }}</td>
  <td>{{ post.title }}</td>
  <td>{{ post.createdAt|date('d-m-Y') }}</td>
</tr>
{% endfor %}
</table>

{% endblock %}
